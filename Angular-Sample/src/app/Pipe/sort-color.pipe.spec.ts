import { SortColorPipe } from './sort-color.pipe';

describe('SortColorPipe', () => {
  it('create an instance', () => {
    const pipe = new SortColorPipe();
    expect(pipe).toBeTruthy();
  });
});
